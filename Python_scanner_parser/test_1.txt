program Fibonacci;

var
    n : int;
    resultado : int;

int fib(num : int)
var
    a, b : int;
{
    if (num < 2) {
        return(num);
    } else {
        return(fib(num - 1)+ fib(num - 2));
    };
};

void imprimirValor(valor : int)
{
    print(valor);
};

main
{
    n = 20;
    resultado = fib(n);
    imprimirValor(resultado);
}
end