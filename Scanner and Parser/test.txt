program TestExpresiones;
var
    a, b, c : int;
    x, y : float;
main
{
    a = 10;
    b = 20;
    
    /* Precedencia (* antes que +) */
    c = a + b * 2; 
    
    /* Paréntesis */
    c = (a + b * 2;
    
    /* Operadores unarios */
    x = -5.0 + +a; 
    
    /* Expresión relacional compleja */
    if ( (a + b) != (c * -1) ) {
        print("Son diferentes");
    } ;
}
end